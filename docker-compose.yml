name: enterprise-rag

services:
  mongo:
    image: mongo:8
    ports:
      - "27017:27017"
    volumes:
      - mongodata:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.runCommand({ping: 1}).ok"]
      interval: 5s
      timeout: 3s
      retries: 10

  api:
    build: ./backend
    environment: &backend-env
      ENVIRONMENT: development
      MONGODB_URL: mongodb://mongo:27017
      MONGODB_DATABASE: enterprise_rag
      PINECONE_API_KEY: ${PINECONE_API_KEY:-}
      PINECONE_INDEX_NAME: ${PINECONE_INDEX_NAME:-enterprise-rag}
      PINECONE_NAMESPACE: ${PINECONE_NAMESPACE:-enterprise-rag}
      PINECONE_EMBEDDING_MODEL: ${PINECONE_EMBEDDING_MODEL:-multilingual-e5-large}
      PINECONE_EMBEDDING_DIMENSIONS: ${PINECONE_EMBEDDING_DIMENSIONS:-1024}
      CORS_ORIGINS: http://localhost:5173
      GROQ_API_KEY: ${GROQ_API_KEY:-}
    ports:
      - "8000:8000"
    depends_on:
      mongo:
        condition: service_healthy

  worker:
    build: ./backend
    command: ["python", "-m", "app.worker"]
    environment: *backend-env
    depends_on:
      mongo:
        condition: service_healthy

volumes:
  mongodata:
